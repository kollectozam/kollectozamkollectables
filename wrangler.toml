name = "kollectozamkollectables"
compatibility_date = "2026-09-14"
pages_build_output_dir = "./frontend"

# Add the production D1 and R2 bindings in Cloudflare Pages settings.
# The binding names required by the code are DB and PRODUCT_IMAGES.
